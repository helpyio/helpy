apiVersion: v1
kind: Service
metadata:
  name: {{ template "helpy-chart.fullname" . }}
  labels:
    app: {{ template "helpy-chart.name" . }}
    chart: {{ template "helpy-chart.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helpy-chart.name" . }}
    release: {{ .Release.Name }}
