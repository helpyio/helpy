<div id="page-title" class="row">
  <div class="col-md-8" id="page-title-left">
    <span>
      <%= render_breadcrumbs('') %>
    </span>
    <% if @page_title.present? %>
    <h2><%= @page_title %></h2>
    <% else %>
    <%= yield :page_title %>
    <% end %>
  </div>
</div>
