<p><%= t('greetings', default: 'Hello') %> <%= @resource.email %>!</p>

<p><%= t('unlock_instructions.account_locked', default: 'Your account has been locked due to an excessive number of unsuccessful sign in attempts') %>.</p>

<p><%= t('unlock_instructions.unlock_link', default: 'Click the link below to unlock your account') %>:</p>

<p><%= link_to <%= t('unlock_instructions.unlock_account', default: 'Unlock my account') %>, unlock_url(@resource, unlock_token: @token) %></p>
