<% title "#{admin_title} #{t(:discussions, default: "Discussions")}" %>

<% content_for :pipeline do %>
<%#= render partial: 'admin/shared/pipeline' %>
<% end %>



<div class="row">
  <div id="ticket-nav" class="hidden-xs col-md-3 hidden-sm ticket-stats">
    <%= render 'admin/topics/ticket_stats' %>
  </div>
  <div class="col-md-9 main-panel" id="tickets">
    <h4 id="ticket-page-title">
      <%= t(:results_found, count: number_to_human(@topics.total_count)) %>
      <span class='more-important'>"<%= params[:q] %>"</span>
    </h4>
    <%= render 'admin/topics/tickets' %>
  </div>
</div>
