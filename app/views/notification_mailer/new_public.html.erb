<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <p>
      <%= t('above_this_line', default: "Make sure your reply appears above this line") %><br/>
      =================================================================<br/>
      <%= t('message_id', default: 'Message ID') %>:<%= @topic.id %></br>
    </p>    
    <p>
      <%= t('notify_new_public', default: 'A new public customer message has been posted:') %>
    </p>
    <p>
      <%= simple_format(@topic.posts.last.body) %>
    </p>
    <p>
      <%= t('to_reply', default: 'To reply, please view this online:') %> <%= admin_topic_posts_url(@topic, host: AppSettings['settings.site_url']) %>
    </p>
  </body>
</html>
