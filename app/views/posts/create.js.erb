$('#posts').append('<%= j render(partial: 'post', locals: { post: @post }) %>');

// Update timestamps
$('.last-active time[data-time-ago]').timeago();

// jQuery hook
//Helpy.ready();
//Helpy.track();
