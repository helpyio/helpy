
<h1>Help</h1>
<ol>
<% @categories.each do |category| %>


	<li><%= category.name.sentence_case  %></li>
	<ol>
	<% category.docs.ordered.active.each do |doc| %>
		<li><%= doc.title.sentence_case %></li>
	<% end %>
	</ol>
<%
end
%>
</ol>

<% @categories.each do |category| %>

	<h3><%= category.name.sentence_case  %></h3>

	<% category.docs.ordered.active.each do |doc| %>
		<h4><%= doc.title.sentence_case %></h4>
		<%= doc.body %>
	<% end %>

<% end %>
