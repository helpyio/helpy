
<h1>Help</h1>
<ol>
<% @categories.each do |category| %>


	<li><%= category.name.titleize  %></li>
	<ol>
	<% category.docs.ordered.active.each do |doc| %>
		<li><%= doc.title.titleize %></li>
	<% end %>
	</ol>
<%
end
%>
</ol>

<% @categories.each do |category| %>

	<h3><%= category.name.titleize  %></h3>

	<% category.docs.ordered.active.each do |doc| %>
		<h4><%= doc.title.titleize %></h4>
		<%= doc.body %>
	<% end %>

<% end %>
